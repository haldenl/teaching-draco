aggregate_weight(aggregate_count,0).

aggregate_weight(aggregate_sum,2).

aggregate_weight(aggregate_mean,1).

aggregate_weight(aggregate_median,3).

aggregate_weight(aggregate_min,4).

aggregate_weight(aggregate_max,4).

aggregate_weight(aggregate_stdev,5).